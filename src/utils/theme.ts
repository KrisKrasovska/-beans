export interface Theme {
  color: {
    primaryColor: string;
    secondaryColor: string;
    bg: string;
    bgBtn: string;
    accentColor: string;
  };
}

export const theme: Theme = {
  color: {
    primaryColor: "#ffffff",
    secondaryColor: "#373737",
    bg: "#903710",
    bgBtn: "#d5612f",
    accentColor: "#ffe85a",
  },
};
